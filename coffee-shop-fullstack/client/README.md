# Client (Frontend)

Xem hướng dẫn cài đặt & chạy đầy đủ tại `README.md` ở thư mục gốc `coffee-shop-fullstack/README.md`.

Tóm tắt nhanh:
```bash
npm install
npm run dev
```
Yêu cầu backend (`../server`) đang chạy tại http://localhost:4000 (hoặc đổi qua `VITE_API_URL` trong `.env`).
